.env
node_modules/
hq/data/
prompts/operator-profile.md
prompts/operator-profile.prev-*
prompts/gov/entity-profile.md
prompts/gov/boilerplate/
*.log
.DS_Store
Thumbs.db
node_modules/
fiverr-assets/
prompts/operator-profile.draft.md
# companion runtime state (regenerated locally; usage/spend + live dashboard feed)
companion/.usage.json
companion/.dashboard.json
companion/.reminders.json
companion/.web-studio.json
companion/.agent-runs.json
companion/.agent-drafts.json
companion/.paper.json
companion/.music.json
# media-generation spend ledger (code-enforced monthly cap state)
scripts/.media-usage.json
# control-plane event store (system of record — local runtime data) + eval results
control-plane/data/
evals/.results.json
# model-router runtime brain-mode toggle (Auto/Local/Claude/OpenRouter — set from the UI chip)
control-plane/brain-mode.json
# vault idea-miner runtime state (regenerated each run; the 💡 Ideas note lives in the vault)
pods/vault/ideas.json
# companion-generated images (work product, regenerated)
companion/public/_generated/
# offline Vosk wake model (~41MB binary — download via scripts/get-vosk-model.mjs)
companion/public/models/
# netlify CLI local link state
.netlify/
# inbox audit/unsubscribe reports contain personal sender lists — keep them local
reports/
# Jarvis World build output + PWA artifacts (regenerated by `vite build`)
jarvis-world/dist/
jarvis-world/dev-dist/
# Docker bind-mount volumes created on the NAS at runtime
volumes/
# local dev tooling (preview launch config, scheduler runtime state)
.claude/
control-plane/data/
# Gov proposal drafts (work product; may contain entity details) — regenerated by the gov worker
gov-drafts/
# Finance drafts (invoice specs + payment-link emails; contain client details) — regenerated by Victor
finance-drafts/
# Local UI reference images + any partial browser downloads (not source)
UI Inspiration/
*.crdownload
desktop/dist/
# encrypted secrets vault (never commit)
control-plane/vault.enc
Biography/

# personal reference media (not code)
jarvis video ideas/

# youtube-triage title cache (generated)
scripts/.yt-titles.json

scripts/.absorb-done.json

# USASpending agency-spending cache (regenerated; refreshed on a TTL)
pods/gov/.spending-cache.json

# Kokoro local-voice model files (~340MB binaries — download once, not committed)
kokoro-v1.0.onnx
voices-v1.0.bin

# Research & Risk desk runtime journal
pods/research-risk/journal.md

# generated deal ledger (per-machine data, like the CRM volume)
pods/gov/deals.json

# self-learned lessons (personal, like the operator profile)
prompts/lessons/

# companion local state (last-seen for catch-up)
companion/data/

# runtime logs from run-loop.cmd
logs/
